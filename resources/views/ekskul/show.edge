<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Data Siswa</title>
  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <style>
    body {
      padding: 20px;
    }
  </style>
  
</head>
<body>
  <h1>https://bit.ly/pptadonisjs</h1>
  

  <div >
    <h3> Ekskul</h3>
    <form id="ekskul" action="{{ route('ekskul.update',{id:ekskul?.id}) }}" method="POST">
      <div style="margin-bottom: 10px">
        <div style="margin-bottom: 10px">
          <label for="nama">Nama ekskul:</label>
          <input type="text" id="nama" name="nama" value="{{ekskul?.nama}}" required />
        </div>
        <label for="kode">Jumlah siswa:</label>
        <input type="number" id="jumlah_siswa" name="jumlah_siswa" value="{{ekskul?.jumlahSiswa}}" required />
      </div>
      <div style="margin-bottom: 15px">
        <label for="Guru">Deskripsi:</label>
        <input type="text" id="deskripsi" name="deskripsi" value="{{ekskul?.deskripsi}}" required />
      </div>
      <button type="submit" style="margin-bottom: 10px">Edit Ekskul</button>
    </form>
   
  </div>
  <h2>Data Ekskul</h2>
  <table class="table">
    <thead>
      <tr>
        <th scope="col">No</th>
        <th scope="col">Nama Siswa</th>
        <th scope="col">Whatsapp</th>
        <th scope="col">Tindakan</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <th scope="row">1</th>
        <td>John Doe</td>
        <td>XII IPA 1</td>
        <td>
          <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#editModal">
            Edit
          </button>
          <button type="button" class="btn btn-danger" data-toggle="modal" data-target="#deleteModal">
            Hapus
          </button>
        </td>
      </tr>
      <!-- Tambahkan baris sesuai dengan data siswa yang dimiliki -->
    </tbody>
  </table>

  <!-- Tombol Tambah Siswa -->
  <button type="button" class="btn btn-success" data-toggle="modal" data-target="#addModal">
    Tambah Siswa
  </button>

  <!-- Modal Tambah Siswa -->
  <div class="modal" id="addModal" tabindex="-1" role="dialog" aria-labelledby="addModalLabel" aria-hidden="true">
    <!-- Isi modal tambah siswa di sini -->
    <!-- Modal Tambah Siswa -->

    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="addModalLabel">Tambah Siswa</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div class="modal-body">
          <!-- Form untuk menambah siswa -->
          <form id="siswa-form" action="{{ route('siswa.store') }}" method="POST">
            <input type="hidden" value="{{ekskul.id}}">
            <div class="form-group">
              <label for="nama">Nama Siswa:</label>
              <input type="text" class="form-control" id="nama" name="nama" required>
            </div>
            <div class="form-group">
              <label for="whatsapp">Whatsapp:</label>
              <input type="text" class="form-control" id="whatsapp" name="whatsapp" required>
            </div>
            <!-- Tambahkan input untuk atribut lain sesuai kebutuhan -->
  
            <button type="button" class="btn btn-secondary" data-dismiss="modal">Tutup</button>
            <button type="submit" class="btn btn-primary">Simpan</button>
          </form>
        </div>
      </div>
    </div>
  
  
  </div>

  <!-- Modal Edit Siswa -->
  <div class="modal" id="editModal" tabindex="-1" role="dialog" aria-labelledby="editModalLabel" aria-hidden="true">
    <!-- Isi modal edit siswa di sini -->
  </div>

  <!-- Modal Hapus Siswa -->
  <div class="modal" id="deleteModal" tabindex="-1" role="dialog" aria-labelledby="deleteModalLabel" aria-hidden="true">
    <!-- Isi modal hapus siswa di sini -->
  </div>

<!-- Bootstrap CSS -->
<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
<script>document.addEventListener("DOMContentLoaded", function () {
  const ekskulForm = document.getElementById("ekskul");
  const ekskulModal = document.getElementById("ekskulModal");
  const overlay = document.getElementById("overlay");

  // ekskulForm.addEventListener("submit", function (event) {
  //   event.preventDefault();

  //   closeModal();
  // });

  window.openModal = function () {
    ekskulModal.style.display = "block";
    overlay.style.display = "block";
  };

  window.closeModal = function () {
    ekskulModal.style.display = "none";
    overlay.style.display = "none";
    ekskulForm.reset();
  };
});</script>
<!-- Bootstrap JS and dependencies -->
<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

</body>
</html>
