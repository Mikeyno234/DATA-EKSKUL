<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Data user</title>
  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <style>
    body {
      padding: 20px;
    }
  </style>
  
</head>
<body>

  

  <div >
    <h3> User</h3>
    <form id="user" action="{{ route('user.update',{id:user?.noUrut}) }}" method="POST">
      <div style="margin-bottom: 10px">
        <div style="margin-bottom: 10px">
          <label for="nim">Nim:</label>
          <input type="text" id="nim" name="nim" value="{{user?.nim}}" disabled/>
        </div>
        <div style="margin-bottom: 10px">
          <label for="nama">Nama user:</label>
          <input type="text" id="nama" name="nama" value="{{user?.nama}}" disabled/>
        </div>
        <label for="umur">Umur:</label>
        <input type="number" id="umur" name="umur" value="{{user?.umur}}" disabled/>
      </div>
        </div>
        <label for="email">Email:</label>
        <input type="email" id="email" name="email" value="{{user?.email}}" disabled/>
      </div>
        </div>
        <label for="status">Status:</label>
        <input type="text" id="status" name="status" value="{{user?.status}}" disabled/>
      </div>
        </div>
        <label for="daftar_ulang">Daftar Ulang:</label>
        <input type="text" id="daftar_ulang" name="daftar_ulang" value="{{user?.daftarUlang}}" disabled/>
      </div>
      
      <button type="submit" style="margin-bottom: 10px">Edit user</button>
    </form>
   
  </div>
  <!-- <h2>Data user</h2>
  <table class="table">
    <thead>
      <tr>
        <th scope="col">No Urut </th>
        <th scope="col">Nim </th>
        <th scope="col">Namat </th>
        <th scope="col">Umur </th>
        <th scope="col">Email </th>
        <th scope="col">Status</th>
        <th scope="col">Daftar Ulang</th>
        <th scope="col">Tindakan</th>
      </tr>
    </thead> -->
    <!-- <tbody>
      <tr>
        <th scope="row">1</th>
        <td>John Doe</td>
        <td>XII IPA 1</td>
        <td>
          <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#editModal">
            Edit
          </button>
          <button type="button" class="btn btn-danger" data-toggle="modal" data-target="#deleteModal">
            Hapus
          </button>
        </td>
      </tr>
      Tambahkan baris sesuai dengan data user yang dimiliki
    </tbody> -->
  </table>

  <!-- Tombol Tambah user -->
  <!-- <button type="button" class="btn btn-success" data-toggle="modal" data-target="#addModal">
    Tambah User
  </button> -->
<!-- 
  Modal Tambah user
  <div class="modal" id="addModal" tabindex="-1" role="dialog" aria-labelledby="addModalLabel" aria-hidden="true">
    Isi modal tambah user di sini
    Modal Tambah user

    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="addModalLabel">Tambah user</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div class="modal-body">
          Form untuk menambah user
          <form id="user-form" action="{{ route('user.store') }}" method="POST">
            <input type="hidden" value="{{user.id}}">
            <div class="form-group">
              <label for="nama">Nim:</label>
              <input type="text" class="form-control" id="nama" name="nama" required>
            </div>
            <div class="form-group">
              <label for="whatsapp">N:</label>
              <input type="text" class="form-control" id="whatsapp" name="whatsapp" required>
            </div>
            Tambahkan input untuk atribut lain sesuai kebutuhan
  
            <button type="button" class="btn btn-secondary" data-dismiss="modal">Tutup</button>
            <button type="submit" class="btn btn-primary">Simpan</button>
          </form>
        </div>
      </div>
    </div>
  
  
  </div> -->

  <!-- Modal Edit user -->
  <div class="modal" id="editModal" tabindex="-1" role="dialog" aria-labelledby="editModalLabel" aria-hidden="true">
    <!-- Isi modal edit user di sini -->
  </div>

  <!-- Modal Hapus user -->
  <div class="modal" id="deleteModal" tabindex="-1" role="dialog" aria-labelledby="deleteModalLabel" aria-hidden="true">
    <!-- Isi modal hapus user di sini -->
  </div>

<!-- Bootstrap CSS -->
<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
<script>document.addEventListener("DOMContentLoaded", function () {
  const userForm = document.getElementById("user");
  const userModal = document.getElementById("userModal");
  const overlay = document.getElementById("overlay");

  // userForm.addEventListener("submit", function (event) {
  //   event.preventDefault();

  //   closeModal();
  // });

  window.openModal = function () {
    userModal.style.display = "block";
    overlay.style.display = "block";
  };

  window.closeModal = function () {
    userModal.style.display = "none";
    overlay.style.display = "none";
    userForm.reset();
  };
});</script>
<!-- Bootstrap JS and dependencies -->
<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

</body>
</html>
